spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:h2:mem:prod}
    driverClassName: org.postgresql.Driver
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:password}

rsa:
  private-key-path: /etc/secrets/RSA_PRIVATE_KEY_PROD